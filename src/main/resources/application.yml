server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5332/users
    username: darko
    password: P
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  main:
    web-application-type: servlet
